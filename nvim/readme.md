For this setup to work, follow the following steps:

1. ```nvim lua/awicher/packer.lua```
2. enter command ```:so``` to source the file
3. enter command ```:PackerSync``` to install the specified plugins
4. quit and reopen neovim
